The class Display is not necessary if the program is run using
the Click class which gathers user input and displays via a simple GUI.


The class Display is if you wish to run the game with plain text
in the terminal.
